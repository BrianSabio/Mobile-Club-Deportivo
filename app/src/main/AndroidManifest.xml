<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Permiso de escritura en almacenamiento externo.
        Necesario para guardar el PDF en Android 7.0 a 9 (API 24 a 28).
        Android 10+ (API 29+) usa MediaStore y no requiere este permiso.
        maxSdkVersion=28 evita solicitar el permiso innecesariamente en versiones modernas.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_club_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_club_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile_club_deportivo">
        <activity
            android:name=".CarnetActivity"
            android:exported="false" />
        <activity
            android:name=".PaymentActivity"
            android:exported="false" />
        <activity
            android:name=".ManageActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterClientActivity"
            android:exported="false" />
        <activity
            android:name=".MainMenuActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>